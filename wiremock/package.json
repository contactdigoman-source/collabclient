{
  "name": "nexval-wiremock",
  "version": "1.0.0",
  "description": "WireMock stubs for Nexval Attendance API",
  "scripts": {
    "start": "node start-with-ip.js",
    "stop": "pkill -f 'wiremock.*8080' || echo 'WireMock not running'",
    "start:docker": "docker-compose up -d",
    "stop:docker": "docker-compose down",
    "logs:docker": "docker-compose logs -f"
  },
  "keywords": [
    "wiremock",
    "api",
    "stubs",
    "mock"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wiremock": "^2.35.0"
  }
}

