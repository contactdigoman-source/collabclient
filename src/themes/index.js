import { DarkThemeColors, LightThemeColors } from './colors';
import { LightTheme } from './light';
import { DarkTheme } from './dark';

const APP_THEMES = {
  dark: 'dark',
  light: 'light',
  default: 'default',
};

export { DarkThemeColors, LightThemeColors, LightTheme, DarkTheme, APP_THEMES };
